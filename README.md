# Installation

On Ubuntu, run:

    sudo apt install cmake libpulse-dev qt6-base-dev qt6-multimedia-dev qt6-svg-dev

to install dependencies, then type

    make
    
# Running the game

    ./xtrism

should do the trick.
